package application.interfaces;

/**
 * This interface is used to define the classes whose javafx part can be refreshed. |
 *   Cette interface sert à définir les classes dont la partie JavaFX peut être actualisée.
 */
public interface refreshable {
    void refresh();
}
